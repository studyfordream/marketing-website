staging:
  image: nginx
  ports:
   - "80:80"
   - "443:443"
  volumes:
   - nginx.conf:/etc/nginx/nginx.conf
   # need to have real or self-signed SSL certs
   # - local.crt:/etc/nginx/ssl/local.crt
   # - local.key:/etc/nginx/ssl/local.key
