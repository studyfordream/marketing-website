---
layout: opticon.hbs
seo_title: Opticon Agenda
no_container: true
no_header: true
no_footer: true
body_class:
- opticon
- opticon-agenda
og_title: "Opticon 2015"
og_description: "Join me at Opticon and learn why experience optimization is a must for your business."
---

<div class="confetti-container">
  <div class="confetti-right"></div>
</div>
{{#with agenda_data}}
<div class="content-container container">
  <h1 class="page-title common-page-title adelle-sans">{{title}}</h1>
  <p class="subtitle adelle-sans">{{sub_title}}</p>

  <div class="agenda-container flex-container">
    <div class="agenda-day">
      <h3 class="description-title adelle-sans">{{first_day.title}}</h3>
      {{#each first_day.times}}
      <div class="agenda-row flex-container">
        <p class="agenda-time adelle-sans">{{{this.time}}}</p>
        <p class="agenda-event adelle-sans">{{this.event}}</p>
    </div>
      {{/each}}
    </div>

    <div class="agenda-day">
      <h3 class="description-title adelle-sans">{{second_day.title}}</h3>
      {{#each second_day.times}}
      <div class="agenda-row flex-container">
        <p class="agenda-time adelle-sans">{{{this.time}}}</p>
        <p class="agenda-event adelle-sans">{{this.event}}</p>
      </div>
      {{/each}}
    </div>
  </div>
</div>
{{/with}}
